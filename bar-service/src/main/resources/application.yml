server:
  port: 8081

spring:
  application:
    name: bar-service

  datasource:
    url: jdbc:postgresql://localhost:5432/bar
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

  rabbitmq:
    host: localhost
    port: 5672
    username: igloo
    password: igloo

  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8099/realms/bulldozer

inventory:
  service:
    url: http://localhost:8082
